{{-- <table>
    <thead>
        <tr>
            <th>Age</th>
            <th>Clinic</th>
        </tr>
    </thead>
    <tbody>
        @foreach ($information as $info)
            <tr>
                <td>{{ $info->age }}</td>
                <td>{{ $info->clinic }}</td>
            </tr>
        @endforeach
    </tbody>
</table>


<a href="{{ route('export.excel') }}" class="btn btn-primary">Export to Excel</a> --}}
